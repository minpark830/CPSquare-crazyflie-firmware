# Follower 2 drone firmware for Crazyflie 2.x

This application is the firmware to uploaded for a tester follower drone

To run this example, compile and flash the app with ```make -j$(nproc) && make cload```.
